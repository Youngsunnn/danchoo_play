extends ../base

block content
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(name="username", placeholder="이름", required, type="text")
        input(name="phoneNum", placeholder="전화번호", required, type="text")
        input(name="showNum", placeholder="관람회차", required, type="text")
        input(type="submit", value="submit")